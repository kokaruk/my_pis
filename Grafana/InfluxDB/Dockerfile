FROM influxdb:1.7.7-alpine

ARG INFLUXDB_ADMIN_PASSWORD
ARG INFLUXDB_USER_PASSWORD

ENV INFLUXDB_DB=db0
ENV INFLUXDB_ADMIN_USER=admin
ENV INFLUXDB_ADMIN_PASSWORD=$INFLUXDB_ADMIN_PASSWORD

COPY influxdb.conf /etc/influxdb/influxdb.conf:ro